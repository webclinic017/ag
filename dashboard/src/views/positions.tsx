import React, { FC } from "react";
import Layout from "../components/Layout";

const Positions:FC = () => {
    return (
        <Layout>
            <h1>Welcome positions</h1>
        </Layout>
    )
}

export default Positions